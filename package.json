{
  "name": "aws-example-api-ts",
  "version": "1.0.0",
  "description": "",
  "dependencies": {
    "express": "^4.19.2",
    "serverless-http": "^3.2.0"
  },
  "scripts": {
    "build": "esbuild src/handler.ts --bundle --outfile=dist/handler.js --platform=node --target=node20",
    "dev": "serverless offline start",
    "deploy": "serverless deploy"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "esbuild": "^0.24.0",
    "serverless": "^4.4.3",
    "serverless-offline": "^14.3.2",
    "typescript": "^5.6.2"
  }
}
